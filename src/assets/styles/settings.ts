// ========================================================================
// CORE SETTINGS
// ========================================================================

export const fontSizePx = 16;
export const lineHeight = 1.5;
export const lineHeightPx = fontSizePx * lineHeight;

export const colors = `
  :root {
    --text-color: hsl(0, 0%, 67%);
    --bg-color: hsl(0, 0%, 100%);
    --theme-primary: hsla(224, 100%, 50%, 1);
  }
`;
